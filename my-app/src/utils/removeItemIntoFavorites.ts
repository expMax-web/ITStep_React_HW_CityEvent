import { FavoritesArr } from "../pages/info/Favorites";

export const removeItemIntoFavorites = (id: string): void => {
  //   const [Favorites, setPointer]: any = useState();
  //   const index = Favorites.findIndex((item: any) => item.id === Number(id));
  //   if (index >= 0) {
  //     setPointer(Favorites.splice(index, 1));
  //   }
  //   console.log(Favorites);
};
